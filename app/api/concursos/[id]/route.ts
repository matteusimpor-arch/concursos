import {NextRequest,NextResponse} from "next/server";
function config(){const url=process.env.SUPABASE_URL,key=process.env.SUPABASE_ANON_KEY;if(!url||!key)throw new Error("Supabase não configurado.");return{url,headers:{apikey:key,Authorization:`Bearer ${key}`,"Content-Type":"application/json"}}}
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,{url,headers}=config(),body=await req.json(),r=await fetch(`${url}/rest/v1/concursos?id=eq.${encodeURIComponent(id)}`,{method:"PATCH",headers:{...headers,Prefer:"return=representation"},body:JSON.stringify({...body,prova:body.prova||null,inscricao_fim:body.inscricao_fim||null,observacoes:body.observacoes||null})});return NextResponse.json(await r.json(),{status:r.status})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Erro ao atualizar."},{status:503})}}
export async function DELETE(_req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const{id}=await params,{url,headers}=config(),r=await fetch(`${url}/rest/v1/concursos?id=eq.${encodeURIComponent(id)}`,{method:"DELETE",headers});return new NextResponse(null,{status:r.status})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Erro ao excluir."},{status:503})}}
